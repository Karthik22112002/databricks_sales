bundle:
  name: databricks_sales_capstone

include:
  - "resources/*.yml"

targets:
  dev:
    default: true
    workspace:
      host: https://dbc-586a4832-3302.cloud.databricks.com

  prod:
    workspace:
      host: https://dbc-586a4832-3302.cloud.databricks.com
      # Use a neutral path or remove root_path override so production defaults safely
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}